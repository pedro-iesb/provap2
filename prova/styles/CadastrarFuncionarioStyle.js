import { StyleSheet } from "react-native";

const CadastrarFuncionarioStyle = StyleSheet.create({
    container: {
        padding: 10
    },

    containerInput: {
        gap: 10,
    }
})

export default CadastrarFuncionarioStyle